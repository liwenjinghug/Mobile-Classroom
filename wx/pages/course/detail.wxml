<view class="container">
  <!-- 课堂信息卡片 -->
  <view class="course-header">
    <view class="course-title">{{course.className || course.title || '课堂详情'}}</view>
    <view class="course-id">课堂ID：{{course.id}}</view>
  </view>

  <!-- 状态提示 -->
  <view wx:if="{{loading}}" class="loading">加载中...</view>

  <!-- 功能入口网格 -->
  <view class="function-grid">
    <view class="function-item" bindtap="goToHomework">
      <view class="function-icon homework-icon">
        <text class="icon-text">📝</text>
      </view>
      <text class="function-label">作业</text>
    </view>

    <view class="function-item" bindtap="goToExam">
      <view class="function-icon exam-icon">
        <text class="icon-text">📋</text>
      </view>
      <text class="function-label">考试</text>
    </view>

    <view class="function-item" bindtap="goToSign">
      <view class="function-icon sign-icon">
        <text class="icon-text">✓</text>
      </view>
      <text class="function-label">签到</text>
    </view>

    
  </view>
</view>
