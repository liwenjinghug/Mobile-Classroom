<view class="container">
  <view>
    <text style="font-size:16px;font-weight:600;">我听的课</text>
    <view wx:if="{{listening.length === 0}}" style="color:#888;margin-top:8px;">暂无加入的课堂，点击下方“加入课堂”或创建课堂</view>
    <block wx:for="{{listening}}" wx:key="id">
      <view class="course-item" bindtap="openCourse" data-id="{{item.id}}" style="padding:10px;border-bottom:1px solid #eee;">
        <text>{{item.title}}</text>
      </view>
    </block>
    <view style="margin-top:12px;">
      <button bindtap="onJoinClass" class="button">加入课堂</button>
    </view>
  </view>
  <view style="margin-top:18px;">
    <text style="font-size:16px;font-weight:600;">我教的课</text>
    <block wx:for="{{teaching}}" wx:key="id">
      <view class="course-item" bindtap="openCourse" data-id="{{item.id}}" style="padding:10px;border-bottom:1px solid #eee;">
        <text>{{item.title}}</text>
      </view>
    </block>
    <view style="margin-top:12px;">
      <button bindtap="onCreateClass" class="button">创建课堂</button>
    </view>
  </view>
</view>

