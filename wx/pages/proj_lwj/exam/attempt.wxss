/* 页面容器 */
.page {
  padding: 20rpx;
  min-height: 100vh;
  background: linear-gradient(180deg, #f7fbff 0%, #f9fbff 100%);
}

/* 装饰性背景 */
.page::before {
  content: '';
  position: fixed;
  top: -100rpx;
  right: -100rpx;
  width: 400rpx;
  height: 400rpx;
  background: radial-gradient(circle, rgba(59, 130, 246, 0.06) 0%, transparent 70%);
  border-radius: 50%;
  pointer-events: none;
  z-index: 0;
}

/* 考试标题 */
.header {
  font-size: 34rpx;
  margin-bottom: 16rpx;
  font-weight: 700;
  color: #303133;
  text-align: center;
  padding: 16rpx 20rpx;
  background: #fff;
  border-radius: 16rpx;
  box-shadow: 0 2rpx 8rpx rgba(0,0,0,0.05);
}

.header::before {
  content: '📝';
  margin-right: 12rpx;
  font-size: 38rpx;
}

/* 倒计时显示（轻量） */
.meta {
  color: #E6A23C;
  margin-bottom: 20rpx;
  font-size: 28rpx;
  font-weight: 600;
  text-align: center;
  padding: 12rpx 16rpx;
  background: #fff7e6;
  border-radius: 12rpx;
  border: 1rpx solid #faecd8;
}

/* 题目卡片（柔和） */
.question {
  background: #fff;
  padding: 20rpx;
  margin-bottom: 20rpx;
  border-radius: 16rpx;
  box-shadow: 0 2rpx 8rpx rgba(0,0,0,0.05);
  border: 1rpx solid #f0f0f0;
}

/* 题目标题 */
.q-title {
  font-weight: 600;
  margin-bottom: 16rpx;
  font-size: 30rpx;
  line-height: 1.6;
  color: #303133;
  padding-left: 8rpx;
  border-left: 4rpx solid #409eff;
  position: relative;
}

/* 选项样式 */
.option {
  display: block;
  padding: 16rpx;
  font-size: 28rpx;
  margin-bottom: 12rpx;
  background: #f9fbff;
  border-radius: 12rpx;
  border: 1rpx solid #e5e9f2;
  transition: all 0.2s ease;
  position: relative;
  overflow: hidden;
}

.option:active {
  transform: translateX(2rpx);
  background: #ecf5ff;
  border-color: #409eff;
}

/* Radio 和 Checkbox 美化 */
radio, checkbox {
  transform: scale(1.1);
  margin-right: 12rpx;
}

/* 文本输入框 */
textarea {
  width: 100%;
  min-height: 180rpx;
  padding: 16rpx;
  border: 1rpx solid #e5e9f2;
  border-radius: 12rpx;
  font-size: 28rpx;
  line-height: 1.6;
  background: #fafbff;
  color: #303133;
  box-sizing: border-box;
}

textarea:focus {
  border-color: #409eff;
  background: #fff;
}

/* 操作区域 */
.actions {
  text-align: center;
  margin: 32rpx 0 40rpx;
}

.actions button {
  width: 86%;
  padding: 24rpx 0;
  font-size: 32rpx;
  font-weight: 700;
  background: linear-gradient(135deg, #1890ff 0%, #66b1ff 100%);
  color: #fff;
  border-radius: 16rpx;
  border: none;
  box-shadow: 0 8rpx 20rpx rgba(24,144,255,0.25);
}

.actions button:active {
  transform: translateY(2rpx);
  opacity: 0.96;
}

/* ===== 防作弊设置 ===== */
/* 禁用选择和长按弹出菜单（除了输入框） */
.page, .question, .q-title, .option {
  -webkit-user-select: none;
  -ms-user-select: none;
  user-select: none;
  -webkit-touch-callout: none;
}

/* 允许在输入区域正常选择和操作 */
textarea {
  -webkit-user-select: text;
  -ms-user-select: text;
  user-select: text;
  -webkit-touch-callout: default;
}

/* ===== 响应式设计 ===== */
@media (max-width: 375px) {
  .header { font-size: 32rpx; padding: 14rpx 16rpx; }
  .q-title { font-size: 28rpx; }
  .option { font-size: 26rpx; padding: 14rpx; }
  .actions button { width: 90%; font-size: 30rpx; }
}
