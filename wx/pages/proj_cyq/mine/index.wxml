<view class="container">
  <view class="user-card">
    <view class="avatar-box" bindtap="handleAvatarClick">
      <image class="avatar" src="{{userInfo.avatar || '/assets/img/profile.png'}}" mode="aspectFill"></image>
      <view class="camera-icon">📷</view>
    </view>
    <view class="info-box">
      <view class="nickname">{{userInfo.nickName || '用户'}}</view>
      <view class="username">账号：{{userInfo.userName}}</view>
      <view class="dept" wx:if="{{userInfo.dept}}">{{userInfo.dept.deptName}}</view>
    </view>
  </view>

  <view class="menu-list">
    <view class="menu-item" bindtap="navigateToPwd">
      <view class="menu-left">
        <text class="menu-icon">🔒</text>
        <text class="menu-text">修改密码</text>
      </view>
      <text class="menu-arrow">></text>
    </view>
    
    <view class="menu-item" bindtap="handleAbout">
      <view class="menu-left">
        <text class="menu-icon">ℹ️</text>
        <text class="menu-text">关于我们</text>
      </view>
      <text class="menu-arrow">></text>
    </view>
  </view>

  <view class="logout-btn" bindtap="handleLogout">退出登录</view>
</view>